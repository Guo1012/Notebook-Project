import { CommandRegistry } from '@lumino/commands';
import { NotebookModel } from '../adapters/docregistry';
import type { NormalizedCell } from '@lumen/nbformat';
import type { SessionContext } from '../adapters/services';
import { Signal } from '@lumino/signaling';
export type InsertableCellType = 'code' | 'markdown' | 'raw' | 'circuit';
export type NotebookMode = 'command' | 'edit';
export declare class NotebookController {
    readonly model: NotebookModel;
    readonly session?: SessionContext | undefined;
    readonly commands: CommandRegistry;
    readonly selectionChanged: Signal<NotebookController, string | null>;
    readonly modeChanged: Signal<NotebookController, NotebookMode>;
    readonly runningChanged: Signal<NotebookController, string | null>;
    readonly executionFailed: Signal<NotebookController, {
        cellId: string;
        error: unknown;
    }>;
    selectedCellId: string | null;
    selectedIds: string[];
    anchorId: string;
    mode: NotebookMode;
    runningCellId: string | null;
    private clipboard;
    private lastKey;
    private lastKeyAt;
    constructor(model: NotebookModel, session?: SessionContext | undefined);
    get selectedCell(): NormalizedCell | undefined;
    selectedCells(): NormalizedCell[];
    private activeIndex;
    private setActive;
    select(id: string | null, event?: MouseEvent): void;
    enterEdit(): void;
    enterCommand(): void;
    moveSelection(delta: -1 | 1, extend?: boolean): void;
    insert(type: InsertableCellType, belowId?: string | null, position?: 'before' | 'after'): NormalizedCell;
    moveCell(id: string, offset: -1 | 1): boolean;
    moveSelected(offset: -1 | 1): boolean;
    duplicateCell(id: string): void;
    deleteCell(id: string): boolean;
    deleteSelected(): boolean;
    copySelected(): void;
    cutSelected(): void;
    cutCell(id: string): void;
    pasteBelow(): void;
    changeSelectedType(type: InsertableCellType): void;
    runSelected(): Promise<void>;
    runCell(id: string): Promise<void>;
    runAll(): Promise<void>;
    clearOutputs(id: string): void;
    handleKeydown(event: KeyboardEvent): void;
    private registerCommands;
}
