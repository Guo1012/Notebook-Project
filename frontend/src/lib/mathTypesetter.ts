/**
 * Application compatibility entry point. MathJax 3 typesetting lives in the
 * installable @lumen/rendermime package so the web app and SDK share one runtime.
 */
export { mathTypesetter, MathTypesetter } from '@lumen/rendermime';
